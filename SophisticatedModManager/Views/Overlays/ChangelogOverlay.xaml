<UserControl x:Class="SophisticatedModManager.Views.Overlays.ChangelogOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:SophisticatedModManager.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:MainViewModel}">
    <Border Visibility="{Binding IsViewingChangelog, Converter={StaticResource BoolToVis}}"
            Background="{StaticResource OverlayDimBrush}"
            HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Border Background="{StaticResource ContentBgBrush}"
                BorderBrush="{StaticResource WoodBrownBrush}"
                BorderThickness="2" CornerRadius="6"
                Padding="24,20"
                HorizontalAlignment="Center" VerticalAlignment="Center"
                MinWidth="450" MaxWidth="600" MaxHeight="500">
            <DockPanel>
                <!-- Header -->
                <Grid DockPanel.Dock="Top" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="{Binding ChangelogModName, StringFormat='Changelog: {0}'}"
                               FontSize="18" FontWeight="Bold"
                               Foreground="{StaticResource WoodBrownBrush}"
                               VerticalAlignment="Center"
                               TextTrimming="CharacterEllipsis"/>
                    <Button Grid.Column="1" Content="X"
                            FontSize="13" FontWeight="Bold"
                            Style="{StaticResource IconButtonDestructive}"
                            Command="{Binding CloseChangelogCommand}"/>
                </Grid>

                <!-- Loading indicator -->
                <TextBlock DockPanel.Dock="Top"
                           Text="Loading changelog..."
                           FontSize="12" Foreground="{StaticResource SubtleTextBrush}"
                           Visibility="{Binding IsLoadingChangelog, Converter={StaticResource BoolToVis}}"
                           Margin="0,0,0,8"/>

                <!-- Close button at bottom -->
                <Button DockPanel.Dock="Bottom"
                        Content="Close"
                        Style="{StaticResource StardewButton}"
                        Command="{Binding CloseChangelogCommand}"
                        Padding="14,5" FontSize="12"
                        HorizontalAlignment="Left"
                        Margin="0,12,0,0"/>

                <!-- Changelog text -->
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <TextBlock Text="{Binding ChangelogText}"
                               FontSize="12" FontFamily="Consolas"
                               Foreground="{StaticResource DarkEarthBrush}"
                               TextWrapping="Wrap"/>
                </ScrollViewer>
            </DockPanel>
        </Border>
    </Border>
</UserControl>
